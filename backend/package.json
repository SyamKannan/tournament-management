{
  "name": "sports-saas-backend",
  "private": true,
  "description": "Node side-dependency for Browsershot poster rendering only — the app itself is Laravel. puppeteer-core (no bundled Chromium download) so this stays small and Alpine-safe; point it at a real Chrome/Chromium via POSTER_CHROME_PATH.",
  "dependencies": {
    "puppeteer-core": "^23.9.0"
  }
}
